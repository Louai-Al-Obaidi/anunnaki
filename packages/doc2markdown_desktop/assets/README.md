# Assets

Place an optional Windows icon named `anunnaki.ico` in this folder to use it in the application and package build. The application deliberately works without an icon asset, so a missing icon cannot prevent a build.
