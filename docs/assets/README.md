# Anunnaki media assets

Place a real application screenshot here as `anunnaki-screenshot.png`.

An optional, short demonstration animation can be added as `anunnaki-demo.gif`. Do not replace these placeholders with mocked product screenshots.
