# Project documentation

- `project/` contains repository configuration and growth guidance.
- `releases/` contains versioned release notes.

Private working materials belong in the ignored `local/` directory and generated
Windows packages belong in the ignored `artifacts/` directory.
